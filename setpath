export SURFPACK=$(pwd)
toolbin="$SURFPACK/tools/bin"
surfbin="$SURFPACK/bin"
newpath=${PATH//$toolbin/}
newpath=${newpath//$surfbin/}
newpath=${newpath//::/:}
newpath="$SURFPACK/tools/bin:$SURFPACK/bin:${PATH//$x/}"
echo "$newpath"
PATH=$newpath


